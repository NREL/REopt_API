apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-redis-nodeport-service
  annotations:
    "helm.sh/resource-policy": keep
spec:
  type: NodePort
  selector:
    app: {{ .Chart.Name }}-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
      nodePort: 30500
