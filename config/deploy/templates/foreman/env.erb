APP_ENV=<%= fetch(:app_env) %>
# Use supervisord's "host_node_name" variable expansion to set this environment
# variable based on the server's hostname.
APP_HOSTNAME=%(host_node_name)s
DEPLOY_CURRENT_PATH=<%= current_path %>
<% if(fetch(:stage) == :vagrant) %>
VAGRANT=true
<% end %>
